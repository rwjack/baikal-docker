name: Build job
on:
  workflow_call:
    inputs:
      dockerfile:
        description: Path to the Dockerfile to build
        required: true
        type: string
      tags:
        description: Tags to apply to the image
        required: true
        type: string
      image-version:
        description: Value for org.opencontainers.image.version label
        required: true
        type: string
      image-revision:
        description: Value for org.opencontainers.image.revision label
        default: ${{ github.sha }}
        type: string
      ref:
        description: Git reference to build
        default: ${{ github.ref }}
        type: string
      assert-version:
        description: When true, require Dockerfile VERSION to match image-version
        default: true
        type: boolean

permissions:
  contents: read
  packages: write
  attestations: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
          show-progress: false

      - name: Assert Dockerfile VERSION matches image-version
        if: inputs.assert-version
        env:
          EXPECTED_VERSION: ${{ inputs.image-version }}
          DOCKERFILE: ${{ inputs.dockerfile }}
        run: |
          DOCKERFILE_VERSION=$(grep -E '^ENV VERSION=' "$DOCKERFILE" | cut -d= -f2 | tr -d '[:space:]')
          if [ -z "$DOCKERFILE_VERSION" ]; then
            echo "::error file=$DOCKERFILE::Could not find ENV VERSION= in Dockerfile"
            exit 1
          fi
          if [ "$DOCKERFILE_VERSION" != "$EXPECTED_VERSION" ]; then
            echo "::error file=$DOCKERFILE::Dockerfile VERSION ($DOCKERFILE_VERSION) does not match image-version ($EXPECTED_VERSION)"
            exit 1
          fi
          echo "Dockerfile VERSION matches image-version: $EXPECTED_VERSION"

      # See https://github.com/docker/setup-qemu-action
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      # See https://github.com/docker/setup-buildx-action
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # See https://github.com/docker/login-action
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # See https://github.com/docker/build-push-action
      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          file: ${{ inputs.dockerfile }}
          platforms: linux/amd64,linux/arm/v7,linux/arm64/v8
          push: true
          provenance: mode=max
          sbom: true
          tags: ${{ inputs.tags }}
          labels: |
            org.opencontainers.image.created=${{ github.event.created_at }}
            org.opencontainers.image.url=https://ghcr.io/${{ github.repository_owner }}/baikal
            org.opencontainers.image.documentation=https://github.com/${{ github.repository }}
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.version=${{ inputs.image-version }}
            org.opencontainers.image.revision=${{ inputs.image-revision }}
            org.opencontainers.image.licenses=MIT
            org.opencontainers.image.title=Baikal
            org.opencontainers.image.description=Ready-to-go Baikal server
